// Test for
// - Function declaration and return type checking
// - Function call argument type and count matching
// - Array argument dimension size checking
// - Variable name conflict with function
// - Implicit type conversions (int <-> float)

int add(int a, int b) {
    return a + b;
}

float multiply(float a, float b) {
    return a * b;
}

string errorTest() {
    return "error test"; // Error: return type is string, but returning int
}

void main() {
    int a = 5;
    int b = 10;
    float c = 2.5;
    float d = 4.0;

    // Function calls
    int sumInt;
    float sumDouble;

    sumInt = add(a, b);             // Valid call
    sumInt = add(a + b, c);         // Implicit conversion from float to int
    sumDouble = add(c, d);          // Implicit conversion: float to int, then result assigned to float

    // Multiplication
    float product;
    product = multiply(c, d);       // Valid call
    print "Product: ";
    println product;
}
